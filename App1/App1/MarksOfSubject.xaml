<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App1.MarksOfSubject">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="marksListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Orientation="Horizontal">
                                    <Label Text="{Binding Value}"></Label>
                                    <Label Text="{Binding Impact}"></Label>
                                    <!--<Button Clicked="Button_Clicked" Text="delete"></Button>
                                    <Label Text="{Binding ID}"></Label>-->
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>